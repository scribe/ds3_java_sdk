package com.spectralogic.escapepod.server.startup

/**
 On startup we need to see if we can join an existing cluster, or if we should startup as the first node in the cluster.
 */
class ClusterStartup {

}